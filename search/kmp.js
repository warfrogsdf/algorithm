/**
 * [-1,-1,0,1,2,-1,0]，这里-1表示不存在，0表示存在长度为1，2表示存在长度为3。
 * @param pStr
 */
function calNext(pStr) {
  let len = pStr.length,
    next = [-1],
    i = 1,
    j,
    k;
  for (; i < len; ++i) {

  }
}

export default function kmp(sStr, pStr) {

}
