/**
 * Created by lib7311 on 2016/6/6.
 */
/**
 * 使用传入的迭代器，对数组进行冒泡排序
 * @param array
 * @param iterator
 * @param type '+'：升序; '-'：降序
 */
module.exports = function (array, iterator, type) {
    var flag = true, //表示还处于无序
        i = 1,
        j = 1,
        l = array.length;
    for(; i < l && true; ++i){
        for(; j < l; ){

        }
    }
};